<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Orders | Swiggy</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        .orders-page-bg {
            background-color: #f7f7f7;
            min-height: 100vh;
        }

        .orders-container {
            max-width: 1000px;
            margin: 0 auto;
            padding: 50px 20px;
        }

        .order-card {
            background: white;
            border: 1px solid #d4d5d9;
            padding: 25px;
            margin-bottom: 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .order-info h3 {
            font-size: 20px;
            color: #282c3f;
            margin-bottom: 5px;
        }

        .order-info p {
            color: #7e808c;
            font-size: 13px;
        }

        .order-status {
            text-align: right;
        }

        .status-badge {
            background: #eee;
            padding: 5px 10px;
            font-size: 12px;
            text-transform: uppercase;
            font-weight: bold;
            color: #535665;
        }

        .order-total {
            font-size: 16px;
            font-weight: bold;
            color: #282c3f;
            margin-top: 5px;
        }
    </style>
</head>

<body class="orders-page-bg">

    {{> header}}

    <div class="orders-container">
        <h2 style="margin-bottom: 30px;">Past Orders</h2>

        {{#each orders}}
        <div class="order-card">
            <div class="order-info">
                <h3>{{this.restaurant_name}}</h3>
                <p>Order #{{this.id}} | {{this.created_at}}</p>
                <div style="margin-top: 10px; font-size: 14px;">
                    <a href="#" style="color: #fc8019; font-weight: bold;">VIEW DETAILS</a>
                </div>
            </div>
            <div class="order-status">
                <span class="status-badge">Delivered</span> <!-- Static for now -->
                <div class="order-total">Total: ₹{{this.total_amount}}</div>
            </div>
        </div>
        {{else}}
        <div style="text-align: center; padding: 50px;">
            <img src="/image/logo.png" style="opacity:0.3; width: 80px;">
            <h3>No orders found</h3>
            <a href="/home?user={{user}}" class="nav-btn-dark" style="margin-top:20px; display:inline-block;">ORDER
                FOOD</a>
        </div>
        {{/each}}
    </div>

    {{> footer}}

</body>

</html>